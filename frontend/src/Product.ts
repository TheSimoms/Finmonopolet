export class Product {
    name: string;
    productNumber: number;

    constructor(name: string, productNumber: number) {
        this.name = name;
        this.productNumber = productNumber;
    }
};
